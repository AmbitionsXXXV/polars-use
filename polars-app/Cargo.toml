[package]
name = "polars-app"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
polars = { workspace = true, features = ["timezones"] }
polars-rw = { version = "0.1.0", path = "../polars-rw" }
utils = { version = "0.1.0", path = "../utils" }
